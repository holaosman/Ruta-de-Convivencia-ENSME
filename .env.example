# Clave del servidor. Nunca la exponga en el frontend.
GEMINI_API_KEY=

# Puede cambiar el modelo si su cuenta usa otro identificador.
GEMINI_MODEL=gemini-3.6-flash

# Token para la ruta privada /admin y el endpoint de reemplazo de manual.
ADMIN_TOKEN=cambie-este-token

PORT=8787
